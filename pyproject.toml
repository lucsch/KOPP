[project]
name = "kopp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "wxpython==4.2.5",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
